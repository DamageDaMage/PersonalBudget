@model HomeViewModel

<h2>My Budgeting App</h2>

<h4>Available Budgets</h4>

<div class="row">
	@foreach (var budget in Model.Budgets)
	{
		<partial name="_BudgetCard" model="budget" />
	}
</div>